---
import BaseLayout from '../../layouts/BaseLayout.astro';
import ArticleCard from '../../components/ui/ArticleCard.astro';
import Breadcrumb from '../../components/ui/Breadcrumb.astro';
import { getCollection } from 'astro:content';

const entries = (await getCollection('spor-kitapligi', ({ data }) => !data.draft))
  .sort((a, b) => b.data.publishDate.getTime() - a.data.publishDate.getTime());
---

<BaseLayout title="Spor Kitaplığı — Tozlu Skor" description="Sporla ilgili kitapların analizleri ve incelemeleri.">

  <!-- Kategori header band -->
  <div class="bg-vintage-dark text-vintage-cream">
    <div class="max-w-6xl mx-auto px-6 py-12 md:py-16">
      <Breadcrumb items={[{ label: 'Spor Kitaplığı' }]} tone="dark" />
      <p class="text-[10px] tracking-[0.35em] uppercase text-vintage-gold/55 mb-4 mt-4">Arşiv</p>
      <h1 class="font-heading text-4xl md:text-5xl font-bold text-vintage-cream leading-tight mb-4">
        Spor Kitaplığı
      </h1>
      <div class="flex items-center gap-3 mb-5">
        <div class="w-8 h-px bg-vintage-gold/40"></div>
        <div class="w-1 h-1 bg-vintage-gold/50 rotate-45 shrink-0"></div>
        <div class="w-16 h-px bg-vintage-gold/20"></div>
      </div>
      <p class="text-vintage-cream/50 text-base max-w-xl font-body leading-relaxed">
        Spor tarihini şekillendiren kitapların incelemeleri, analizleri ve toplumsal perspektiften okumaları.
      </p>
    </div>
  </div>

  <!-- İçerik -->
  <div class="max-w-6xl mx-auto px-6 py-12">
    {entries.length === 0 ? (
      <p class="text-vintage-muted text-center py-16">Henüz içerik eklenmemiş.</p>
    ) : (
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-5">
        {entries.map((entry) => (
          <ArticleCard
            title={entry.data.title}
            description={entry.data.description}
            publishDate={entry.data.publishDate}
            slug={entry.id}
            category="spor-kitapligi"
            meta={`${entry.data.bookTitle} — ${entry.data.bookAuthor}`}
            coverImage={entry.data.coverImage}
          />
        ))}
      </div>
    )}
  </div>
</BaseLayout>
